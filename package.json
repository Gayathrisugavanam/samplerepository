{
  "name": "cypressnew",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run",
    "cy:parallel" : "cypress-parallel -s cy:run -t 2 -d 'cypress/e2e/dragdrop.cy.js' -a '\"<your-cypress-cmd-args>\"'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.3.1",
    "cypress": "^15.0.0",
    "cypress-drag-drop": "^1.1.1",
    "cypress-iframe": "^1.0.1"
  },
  "dependencies": {
    "cypress-parallel": "^0.15.0",
    "xlsx": "^0.18.5"
  }
}
